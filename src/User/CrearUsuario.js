import { View, Text } from 'react-native'
import React from 'react'
import Layout from '../components/Layout'

const CrearUsuario = () => {
  return (
    <Layout>
      <Text>CrearUsuario</Text>
    </Layout>
  )
}

export default CrearUsuario